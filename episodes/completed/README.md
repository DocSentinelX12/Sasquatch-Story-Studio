# Completed Episode Records

There are no completed episodes yet.

When a real episode is finished, place one immutable JSON record per released revision in `records/`, using `episodes/_templates/completed-record.json`. The record should point to the final master and supporting assets through stable asset IDs/checksums; binary media should not be committed here.
