# Sasquatch Story Studio — application dependencies
# Install with:  python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
fastapi>=0.111
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.7
python-multipart>=0.0.9
aiofiles>=23.2
