name: GPU Capability TDD Focused Verification

on:
  push:
    branches:
      - gpu-capability-architecture

permissions:
  contents: read

jobs:
  focused:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - name: Install package and test runner
        run: |
          python -m pip install -e .
          python -m pip install pytest
      - name: Run worker capability binding tests
        run: python -m pytest -q tests/test_gpu_worker_agent.py tests/test_gpu_worker_agent_remote.py tests/test_worker_control_plane.py tests/test_worker_registry_gpu_persistence.py
