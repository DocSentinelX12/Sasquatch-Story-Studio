name: GPU Capability TDD Focused Verification

on:
  push:
    branches:
      - gpu-capability-architecture

permissions:
  contents: read

jobs:
  focused:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - name: Install package and test runner
        run: |
          python -m pip install -e .
          python -m pip install pytest
      - name: Run placement capability tests
        run: python -m pytest -q tests/test_gpu_capability_placement.py tests/test_gpu_scheduler.py tests/test_gpu_aware_scheduler.py tests/test_distributed_gpu_scheduler.py
