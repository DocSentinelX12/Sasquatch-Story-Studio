name: Install LTX-2.5

on:
  workflow_run:
    workflows: ["Install All Studio Engines"]
    types: [completed]
    branches: [main]

permissions:
  contents: read

jobs:
  install:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.event.workflow_run.head_sha }}
      - name: Verify triggering commit
        env:
          EXPECTED_SHA: ${{ github.event.workflow_run.head_sha }}
        run: |
          actual_sha="$(git rev-parse HEAD)"
          if [ "$actual_sha" != "$EXPECTED_SHA" ]; then
            echo "Triggering commit mismatch: expected $EXPECTED_SHA, checked out $actual_sha"
            exit 1
          fi
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Install and verify pinned LTX-2.5 source integration
        run: python scripts/install_ltx25.py
      - name: Run targeted LTX-2.5 contract tests
        run: |
          python -m pip install --upgrade pip pytest
          python -m pip install -e .
          python -m pip check
          python -m pytest -q tests/test_ltx25_runtime.py tests/test_engine_adapters.py
      - name: Upload installation evidence
        uses: actions/upload-artifact@v6
        with:
          name: ltx-2-5-installation-evidence
          path: engine-installations/ltx-2.5/installation-evidence.json
          if-no-files-found: error
